/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','imgs.search.brave.com'], // Add the Unsplash domain here
      },
};

export default nextConfig;
